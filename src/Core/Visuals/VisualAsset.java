package Core.Visuals;

/**
 * first time using a record, its an implicit way its a transparent holder for data
 * @param key
 * @param isAnimation
 */
public record VisualAsset(String key, boolean isAnimation) {}
